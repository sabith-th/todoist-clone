import React from 'react';

const Header = () => (
  <header className="header" data-testid="header">
    <nav>
      <div className="logo">
        <img src="" alt="Todoist" />
      </div>
    </nav>
  </header>
);

export default Header;
